# 차림 — 핵심 기능 이식 명세

CHICODE에서 **홈(오늘의 이슈 제외) · 수업자료 · Lab(→ 동아리) · 일정 · 교사 로그인**
다섯 기능을 차림으로 옮기기 위한 작업 명세다.

조사 기준: CHICODE `main` 브랜치 커밋 `4c70b8b` (2026-08-22), 작업 트리 깨끗함.
경로는 별도 표기가 없으면 `C:\Users\user\Desktop\chicode\web\` 기준이다.

전체 방향과 결정 사항은 [`CLAUDE.md`](CLAUDE.md)에 있다. **이 문서는 데이터 모델에
관해 `CLAUDE.md` 4절보다 우선한다** — 아래 2절에 그 이유를 적었다.

---

## 0. CHICODE 최근 변경 (이식 전 확인)

이 문서를 쓰기 직전 며칠 사이에 들어간 것들이다. 오래된 기억으로 작업하면 놓친다.

| 커밋 | 내용 |
|---|---|
| `315f23e` → `6df0e75` | **수업기록 기능 전체가 새로 들어옴** — 반별 학생 명단, 날짜별 참여 기록, 반 탭, 드래그 정렬, 반 이름 수정, 반 메모, 일괄 추가 |
| `a724128` | 교사 로그인 시 **준비 중인 활동·시즌도 미리보기** 가능 (`listActivities`의 `includePreparingSeason`) |
| `64e3bdd` | 활동 상세의 "← 활동 목록"이 원래 보던 시즌으로 돌아감 |
| `2b2672e` | 활동 항목 첨부에 CSV 허용 |
| `fd1473f` 외 | 개인정보처리방침·이용약관에 "일정"(시간표·수업기록) 반영 |

**수업기록은 완성된 지 얼마 안 된 기능이다.** 차림의 핵심 기능이므로 이식 시
`classRecords.ts`와 `ClassRecords.tsx`를 가장 최신 상태로 가져올 것.

---

## 1. 이식 범위

### 가져온다

| 기능 | 차림에서의 이름 | 경로 |
|---|---|---|
| 홈 | 홈 | `/` |
| 수업자료 | 수업자료 | `/materials`, `/materials/:courseId` |
| Lab | **동아리** | `/club` |
| 일정 | 일정 | `/schedule` |
| 교사 로그인 | 교사 페이지 | `/teacher` |

### 안 가져온다

- **오늘의 AI·IT 이슈(뉴스)** — 홈 히어로의 버튼, `/news`, `TeacherNews.tsx`,
  `newsCandidates`/`newsIssues` 컬렉션, `scripts/fetch-news.mjs`,
  GitHub Actions 자동 수집까지 전부 제외
- **실습 전부** — `/practice`, Python(Pyodide), C(`src/c/**`), Pico(MicroPython),
  Monaco 에디터, `scripts/sync-*.mjs`
- **프로젝트** — ComingSoon 자리표시자였음
- 치즈 브랜딩 일체 (색·Jua 폰트·마스코트·히어로 이미지)

### 이름 바꾸기

| CHICODE | 차림 | 비고 |
|---|---|---|
| Lab | 동아리 | 탭 이름, 화면 문구 전부 |
| `/lab` | `/club` | |
| `/timetable` | `/schedule` | CHICODE는 탭 이름만 "일정"으로 바꾸고 URL은 `/timetable`로 뒀다. 차림은 새로 만드니 URL도 맞춘다 |
| `subjectId` | `courseId` | "과목"이 `subjects`인데 학교 맥락에서 `courses`가 더 맞다 |
| `teachers/{email}` | `members/{uid}` | 아래 5절 |

---

## 2. 데이터 모델

```
workspaces/{wsId}
  members/{uid}              { email, name, role: 'owner' | 'teacher', createdAt }
  settings/site              { schoolName, ... }
  settings/club              { todayMissionText, featuredActivityIds[], pin, updatedAt }

  teacherPages/{slug}        { uid, displayName, published }   ← 교사 공개 주소
                                                                (문서 id = 슬러그)

  courses/{courseId}         { name, pin, pinRequired, published, order,
                               notionUrl, otUrl, otPresentations[], ownerUid }

  materials/{materialId}     { title, description, filename, mimeType, size,
                               chunkCount, createdAt, uploadedBy, courseId }
    chunks/{n}               { data: base64 조각 }

  seasons/{seasonId}         { title, emoji, status, order, description, courseId? }
  activities/{activityId}    { title, seasonId, difficulty, order, published,
                               sections[], materialUrl, courseId?,
                               createdAt, updatedAt, updatedBy }
    files/{sectionId}        항목 첨부 + chunks/{n}
    slides/{fileId}          발표자료 PPTX/PDF + chunks/{n}
  presentations/{activityId} { active, currentSlide, updatedAt }

  classes/{classId}          { name, order, memo, teacherUids[] }   ← 담당 교사 목록
    students/{studentId}     { studentNumber, name, order }
    dates/{dateId}           { date, records: { [studentId]: boolean } }

  timetables/{uid}           { periods, cells, classColors, periodTimes }
```

### `CLAUDE.md` 4절과 달라진 점 — 그리고 그 이유

`CLAUDE.md`에는 수업내용을 `courses/{courseId}/units`로 과목 밑에 넣어뒀는데,
**실제 CHICODE 코드를 읽어보니 그러면 안 된다.**

CHICODE는 `labs`/`labSeasons` 컬렉션 하나를 **두 맥락에서 공유**한다.
`subjectId` 필드가 있으면 과목의 "수업목차/내용"이고, 없으면 Lab 전역의 "시즌/활동"이다.
그래서 `LabRoadmap` / `LabActivities` / `LabActivityDetail` **세 화면을 복제하지 않고
`main.tsx`에서 두 곳에 마운트**하는 것만으로 재사용한다(`lib/labScope.ts`).

과목 밑에 중첩시키면 이 구조가 깨져서 동아리용 화면을 따로 만들어야 한다.
**컬렉션은 평평하게 두고 `courseId` 유무로 스코프를 가른다.** CHICODE에서 이미
검증된 구조다.

### 반드시 지킬 것

- **최상위 전역 컬렉션과 고정 id 싱글턴을 만들지 않는다.**
  CHICODE의 `timetable/default`는 교사가 여럿이면 서로 덮어쓴다 → `timetables/{uid}`.
  `labSettings/home` → `settings/club`.
- `wsId`는 당분간 상수 하나로 고정한다. 경로만 미리 중첩해두는 것이 목적이다.
- **파일은 Firestore에 base64 조각으로 저장한다**(`chunkedFile.ts` 그대로).
  Firebase 새 프로젝트에서 Cloud Storage는 Blaze 유료 플랜을 요구한다.
  Firestore 문서 1MiB 한도 때문에 조각내는 것이다.

---

## 3. 화면 구조 (라우트)

```
/                          홈
/t/:slug                   교사 공개 페이지 — 그 교사의 과목만 (4.6절)
/materials                 과목 선택 (카드 목록, 학교 전체)
/materials/:courseId       ← 과목 게이트(공개 여부 + 핀). <Outlet/> 을 연다
    index                  수업목차 (시즌 카드 그리드)
    /ot                    OT (iframe + OT 발표자료)
    /materials             자료 목록
    /content               내용 목록
    /content/:id           내용 상세
/club                      ← 동아리 게이트(핀). <Outlet/> 을 연다
    index                  동아리 홈 (오늘의 미션 + 추천 활동)
    /seasons               시즌 카드 그리드
    /activities            활동 목록
    /activities/:id        활동 상세
/schedule                  일정 — 시간표 / 기록 두 섹션 (교사만)
/teacher                   교사 페이지
```

### 게이트가 `<Outlet/>` 을 여는 패턴이 핵심이다

핀 게이트를 **부모 라우트**로 두면 `/club/activities` 처럼 자식 경로로 직접
들어와도 핀을 건너뛸 수 없다. 화면마다 핀 검사를 넣는 방식보다 안전하고 단순하다.
CHICODE `LabGate.tsx` / `SubjectMaterials.tsx` 참고.

### 내비게이션

`App.tsx`의 `TABS` 배열이 상단 탭이다. 차림은:

```
홈 · 수업자료 · 동아리        ← 항상
일정                          ← 로그인한 교사에게만 (TEACHER_TABS)
[교사 페이지]                 ← 우측 끝 버튼, 로그인 시 초록 점 표시
```

일정 탭을 교사에게만 보이는 건 편의일 뿐이다 — **실제 방어선은 `firestore.rules`**
에서 `timetables`/`classes` 읽기를 막는 것이다.

한글은 단어 사이 공백이 없어서 flex 아이템이 좁아지면 글자 단위로 줄바꿈된다
(수/업/자/료 세로로 쌓임). `min-w-0` + `overflow-x-auto` + 각 탭 `whitespace-nowrap`
조합으로 막아야 한다 — CHICODE가 실제로 겪고 고친 부분이다.

---

## 4. 기능별 명세

### 4.1 교사 로그인

**원본**: `src/auth/AuthProvider.tsx`, `src/lib/firebase.ts`, `src/pages/Teacher.tsx`

Firebase Auth Google 로그인 + Firestore 화이트리스트. Google 로그인에 성공했다고
교사가 되는 게 아니라, 멤버 문서가 있어야 한다.

```ts
type TeacherState = 'loading' | 'anonymous' | 'not-allowed' | 'teacher'
```

CHICODE는 `teachers/{email.toLowerCase()}` 문서 존재로 판정한다.
**차림은 `workspaces/{wsId}/members/{uid}` 로 바꾼다** — 문서 id가 이메일이 아니라
uid다. 처음 한 번은 본인이 로그인해서 uid를 확인한 뒤 Firebase 콘솔에서 문서를
직접 만들어야 한다.

`firebase.ts`에서 **지우면 안 되는 것 두 가지**:

1. `setPersistence(auth, browserLocalPersistence)` — 기본값(IndexedDB)이 아이패드
   Safari의 `signInWithPopup`과 충돌한다. 팝업이 뜨면 원래 탭이 hidden 처리되고,
   Safari가 배경 탭의 IndexedDB 연결을 강제로 닫아 로그인 결과 저장이 조용히
   실패한다. 아이폰에서는 재현되지 않고 아이패드에서만 난다.
2. `googleProvider.setCustomParameters({ prompt: 'select_account' })` — 계정을
   여러 개 쓰는 교사가 매번 고를 수 있도록.

**교사 페이지 탭 구성** (CHICODE `Teacher.tsx`의 `section` state):
CHICODE는 `materials | lab | news | practice` 네 개다. 차림은 뉴스·실습을 빼고
**`courses | club | 설정`** 으로 간다.

### 4.2 홈

**원본**: `src/pages/Home.tsx`, `src/components/FeatureCard.tsx`

구성은 단순하다.

1. **히어로** — 배경 이미지(알파 페이드) + 큰 제목 + CTA 버튼
   → **"오늘의 AI·IT 이슈" 버튼은 제거한다.**
2. **기능 카드 그리드** — `FeatureCard` 3개. 차림은 수업자료 / 동아리 / 일정으로.

**차림에서는 홈을 두 벌로 나눠야 한다** — 브랜딩 가이드 14절의 요구다.

- **교사용**: "좋은 아침입니다, 선생님!" + 오늘의 수업, 담당 학급, 미작성 기록,
  최근 자료, 오늘의 시간표
- **학생용**: "오늘의 수업" + 시간표 보기 / 수업 내용 보기 / 자료 보기 카드

로그인 상태(`authState === 'teacher'`)로 갈라서 같은 라우트에서 다른 화면을
그리면 된다. **교사용 홈이 이 제품의 실질적인 첫 화면**이므로 CHICODE의 홈을
그대로 베끼지 말고 새로 설계할 것.

### 4.3 수업자료

**원본**: `src/pages/Materials.tsx`, `src/pages/SubjectMaterials.tsx`,
`src/lib/subjects.ts`, `src/lib/materials.ts`, `src/lib/chunkedFile.ts`

`/materials`는 과목 카드 목록, `/materials/:courseId`는 게이트 + 탭 화면이다.

**과목(`courses`) 필드**

| 필드 | 뜻 |
|---|---|
| `name` `order` | 이름, 정렬 순서 |
| `pin` | 핀번호 |
| `pinRequired` | `false`면 핀 없이 열람. 수업 중 핀 오타로 시간 잡아먹을 때 교사가 임시로 푸는 스위치 |
| `published` | `false`면 준비 중 — 이름은 보이지만 못 들어감 |
| `notionUrl` | 새 창으로 여는 외부 링크 |
| `otUrl` | OT 탭에 **iframe으로 띄울** 주소. 비면 OT 탭 자체가 안 보임 |
| `otPresentations[]` | OT 탭 안의 발표자료 목록 (제목만; 실제 파일은 별도 문서) |

> `pinRequired` / `published` 는 **필드가 아예 없는 기존 문서**를 각각 "핀 필요",
> "이미 공개"로 취급하는 정규화 함수가 있다. 차림은 처음부터 만드니 기본값을
> 명시적으로 넣으면 이 분기는 필요 없다.

> `otUrl`은 임베드를 막는 사이트(`X-Frame-Options` / CSP `frame-ancestors`)를 넣으면
> 빈 화면만 나온다. 등록 전에 확인할 것.

**자료(`materials`)** — 목록은 `where('courseId', '==', ...)` 로 거르고 **정렬은
클라이언트에서** 한다. Firestore 복합 색인을 피하기 위한 의도적 선택이다.

**파일 저장** — `chunkedFile.ts`의 `saveChunkedFile` / `loadChunkedFile` /
`deleteChunkedFile`. 문서 참조 하나만 받으므로 자료·발표자료·항목 첨부가 전부
같은 함수를 쓴다. 저장 위치를 Storage로 옮길 때 손댈 곳이 여기뿐이다.

### 4.4 동아리 (구 Lab)

**원본**: `src/lib/labs.ts`, `src/pages/LabGate.tsx`, `LabHome.tsx`,
`LabRoadmap.tsx`, `LabActivities.tsx`, `LabActivityDetail.tsx`,
`LabBoardEditor.tsx`, `src/lib/labScope.ts`

**가장 값나가는 자산이다.** 활동 하나가 곧 수업 하나이고, 본문은 자유로운 항목
배열이다.

```ts
interface Activity {
  title: string
  seasonId: string          // 비면 시즌 미소속 — 목록에만 보임
  difficulty: number
  order: number
  published: boolean        // false면 준비 중
  sections: Section[]
  materialUrl: string
  courseId?: string         // 있으면 과목 스코프, 없으면 동아리 스코프
  createdAt, updatedAt, updatedBy
}

interface Section {
  id: string
  title: string             // "오늘의 목표", "준비물" 등 교사가 자유롭게
  content: string
  isCode: boolean           // true면 구문 강조 + 복사 버튼, 아니면 일반 문단
  kind?: 'slides' | 'checklist'
  items?: ChecklistItem[]   // kind==='checklist'
  hasAttachment?: boolean
  videoUrl?: string
}

interface Season {
  title: string
  emoji: string
  status: '진행중' | '준비중' | '완료'
  order: number
  description: string
  courseId?: string
}
```

**고정 카테고리를 두지 말 것.** CHICODE는 처음에 Arduino/Pico/IoT/AI/Project 다섯
개를 enum으로 박았다가 뺐다 — 교사가 그 다섯 칸 밖의 활동을 못 넣기 때문이다.
**시즌 자체가 곧 분류다.**

**항목이 배열인 이유도 같다.** 예전엔 goal/learn/prep/circuit/code/practice/
mission/challenge가 고정 필드였는데, 교사가 이름을 바꾸고 추가하고 드래그로
순서를 바꿔야 해서 배열로 바꿨다.

**`kind: 'slides'`** 는 발표자료 자리를 표시하는 특수 항목이다. 내용은 별도 문서에
저장되지만 "몇 번째 순서에 보일지"는 다른 항목과 똑같이 배열 위치로 정해진다 —
그래야 드래그로 발표자료 위치도 옮길 수 있다. 활동마다 정확히 하나만 있고
교사가 지울 수 없으며, 없으면 정규화 함수가 맨 끝에 자동으로 채워 넣는다.

**`kind: 'checklist'`** 의 체크 상태는 **교사가 미리 정해두는 안내판**이다. 학생은
못 바꾼다 — 학생마다 다른 상태를 저장할 로그인/저장소가 없어서다.

**조회 옵션** `listActivities({ seasonId, publishedOnly, includePreparingSeason, courseId })`
— `includePreparingSeason`은 로그인한 교사가 준비 중인 것도 미리 볼 수 있게 하는
최근 추가분이다.

**동아리 홈 설정** (`settings/club`): `todayMissionText`(오늘의 미션),
`featuredActivityIds[]`(추천 활동), `pin`(게이트 핀).

**스코프 훅** — `labScope.ts`가 `useParams()`로 `courseId` 유무를 보고 화면 문구와
경로를 계산한다. 과목 맥락이면 "수업목차/내용", 동아리 맥락이면 "시즌/활동".
react-router는 상위 라우트의 param을 자식에서도 그대로 주므로, `main.tsx`에서
같은 컴포넌트를 두 경로 아래 마운트하기만 하면 된다.

**발표 모드** — `presentations/{activityId}` 문서에 현재 슬라이드 번호를 두고
`onSnapshot`으로 학생 화면이 따라온다. 학생은 로그인이 없어 반/교실을 구분할
방법이 없으므로 **활동 하나당 전역으로 하나**다. 사용자가 확인하고 받아들인
트레이드오프다. 슬라이드를 넘기는 권한은 `firestore.rules`에서 막는다.

### 4.5 일정

**원본**: `src/pages/Timetable.tsx`, `src/pages/ClassRecords.tsx`,
`src/lib/timetable.ts`, `src/lib/classRecords.ts`

한 화면 안에 **시간표 / 기록** 두 섹션을 탭으로 전환한다.

#### 시간표

```ts
TIMETABLE_DAYS = ['월','화','수','목','금']
DEFAULT_PERIODS = 7   // 1~10 사이 조절 가능

interface Cell { subject, className, room, note }
interface Data {
  periods: number
  cells: Record<string, Cell>        // 키: `${dayIndex}-${period}` (0=월, period는 1부터)
  classColors: Record<string, string> // 반 이름 → hex
  periodTimes: Record<number, string> // 교시 → "09:00" 같은 자유 텍스트
}
```

칸이 최대 5×10이라 컬렉션으로 쪼개지 않고 **문서 하나를 통째로 읽고 쓴다.**

**같은 반 이름은 항상 같은 색으로 보인다** — 이름을 해시해서 팔레트에서 뽑는
`autoClassColor()`. 새로고침마다 색이 바뀌면 오히려 헷갈리기 때문이다. 교사가
직접 지정하면 `classColors`가 우선한다.

`periodTimes`는 형식을 강제하지 않는다 — "09:00"이든 "09:00~09:50"이든 교사가
쓰는 대로 둔다.

**차림에서 바꿀 것**: 문서 id를 `default`에서 `{uid}`로. 시간표는 교사마다 다르다.

#### 수업기록

```ts
classes/{classId}          { name, order, memo, teacherUids: string[] }
  students/{studentId}     { studentNumber, name, order }
  dates/{dateId}           { date, records: { [studentId]: boolean } }
```

- 반 탭 **드래그 정렬**(`@dnd-kit`), 반 이름 수정, 반 메모
- **일괄 추가** — 텍스트를 붙여넣어 여러 학생을 한 번에. 중복/형식오류를 세어
  `{ added, skippedDuplicate, skippedInvalid }` 로 돌려준다
- 날짜별 참여 여부를 체크박스로 기록

**차림에서 바꿀 것**: 반 명단을 **학교 공용**으로 둔다. CHICODE는 교사 1명이라
구분이 없었지만, 차림은 한 교사가 만든 반 명단을 동료가 그대로 쓰는 것이
이 설계의 실질적 이득이다.

**열람 범위 — 결정됨 (2026-08-22): 담당 교사는 담당 반만 본다.**

그래서 `classes` 문서에 `teacherUids: string[]` 을 둔다. 소유자 한 명이 아니라
**목록**인 이유는 한 반에 국어·수학·영어 교사가 함께 들어가기 때문이다. 그 셋이
명단 하나를 나눠 쓰고, 그 반에 안 들어가는 교사에게는 목록에조차 안 뜬다.

Firestore 규칙은 목록 조회를 대신 걸러주지 않는다 — 조건에 맞지 않는 문서가
하나라도 결과에 들어가면 질의 전체가 거부될 뿐이다. 그러니 화면 쪽 질의에
반드시 제약을 넣어야 한다.

```ts
query(collection(db, ...wsPath('classes')),
      where('teacherUids', 'array-contains', uid))
```

> **아직 확인 못 한 것 — 참여 기록도 담당 교사끼리 공유하나?**
>
> 명단(`students`)을 공유하는 건 분명하다. 그런데 `dates` 의 참여 기록까지
> 공유하면 국어 시간 기록과 수학 시간 기록이 한 문서에서 섞인다. 같은 반이어도
> 교사마다 다른 시간에 다른 걸 기록하므로 **기록은 교사별로 갈라야 맞다**고 본다:
>
> ```
> classes/{classId}/students/{studentId}      ← 담당 교사 공용 (명단)
> classes/{classId}/records/{uid}/dates/{id}  ← 교사 개인 (참여 기록)
> ```
>
> 사용자 확인 전까지 확정하지 말 것. 4단계 시작 전에 물어보면 된다.

---

### 4.6 교사 공개 페이지 (`/t/:slug`)

**CHICODE 에 없던 기능이다.** CHICODE 는 교사가 한 명이라 학생이 갈 곳이 하나뿐
이었다. 차림은 교사가 여럿이고, **각 교사의 학생은 그 교사 것만 봐야 한다.**

#### 왜 필요한가

`/materials` 하나에 학교의 모든 과목을 늘어놓으면 교사 5명 x 2~3과목 = 카드가
15개쯤 된다. 학생 입장에서 "2학년 국어"가 세 개 보이면 어느 게 자기 선생님
건지 모른다. 교사가 매번 "위에서 네 번째"라고 말해줘야 한다.

실제 교실에서 어떻게 쓰이는지가 판단 기준이다 — 교사는 수업 첫날 칠판이나
PPT 에 **주소 한 줄**을 적는다. 그 한 줄이 짧고 고정돼 있어야 한다.

```
charim.vercel.app/t/kim
```

#### 데이터

```
workspaces/{wsId}/teacherPages/{slug}   { uid, displayName, published }
```

**문서 id 가 곧 슬러그다.** 이게 중복 방지 장치다.

클라이언트에서 "이미 있나 확인하고 없으면 저장"은 쓰지 말 것 — 두 교사가 동시에
같은 슬러그를 저장하면 둘 다 확인을 통과한다. 문서 id 로 두면 이미 있는 슬러그에
대한 저장은 생성이 아니라 **수정**이 되고, 규칙의
`resource.data.uid == request.auth.uid` 가 서버에서 거부한다. 검사 로직 없이
구조로 막힌다.

**이메일을 넣지 말 것.** 이 컬렉션을 따로 만드는 이유 자체가 `members` 를 공개
읽기로 열지 않기 위해서다(5절 — 교사 이메일 유출 방지). 여기에 이메일을 담으면
그 우회로를 직접 뚫는 셈이다.

교사 화면에서 자기 슬러그를 찾을 때는 `members/{uid}` 에 따로 복사해두지 말고
`where('uid', '==', myUid)` 로 질의한다. 어차피 공개 읽기라 추가 비용이 없고,
두 곳에 같은 값을 두면 어긋난다.

#### 과목 목록은 `ownerUid` 로 거른다

새 필드가 필요 없다. `courses` 에 이미 `ownerUid` 가 있다.

```ts
query(collection(db, ...wsPath('courses')), where('ownerUid', '==', uid))
```

정렬은 클라이언트에서 한다 — `where` + `orderBy` 조합은 복합 색인을 요구한다
(8절 5번).

#### 과목 상세는 `/materials/:courseId` 그대로 쓴다

`/t/kim/korean-2` 처럼 과목까지 교사 주소 밑에 넣지 않는다. 그러려면 과목에도
슬러그가 필요하고 중복 관리를 한 벌 더 해야 하는데, 학생이 외우는 주소는
교사 페이지 하나면 충분하다. `/t/:slug` 는 **목록만** 보여주고 카드를 누르면
기존 과목 게이트로 보낸다.

#### 핀은 과목 단위 그대로

교사 페이지에 게이트를 한 겹 더 씌우면 학생이 핀을 두 번 넣는다. `/t/:slug` 는
열어두고 과목 **이름만** 보인다 — `courses` 가 이미 `read: true` 인 것과 같은
수준이다. 자료 본문에 들어가려면 여전히 과목 핀이 필요하다.

핀이 "가벼운 잠금"이라는 트레이드오프는 5절 그대로다. 교사 페이지를 나눈다고
강해지지도 약해지지도 않는다.

#### 슬러그 제약과 변경

- **영문·숫자·하이픈만.** `/t/김선생` 은 주소창에서 `%EA%B9%80...` 으로 깨져
  보여서 학생에게 불러주기 어렵다.
- **바꾸면 학생들이 아는 주소가 죽는다.** 학기 중에 바꾸면 수백 명이 404 를 본다.
  막지는 말되 저장 전에 경고를 띄운다. 실제로 문제가 되면 옛 문서를 지우지 않고
  `{ redirectTo: 'new-slug' }` 만 남겨 이전 주소를 살리는 방법이 있다 —
  처음부터 만들지는 말 것.
- `displayName` 은 실명이 아니어도 된다(`국어과 김샘`). 공개 읽기라 학생 아닌
  사람도 볼 수 있다는 걸 교사에게 알려주고 정하게 한다.

#### 학생용 홈에 교사 목록

주소를 잊은 학생을 위해 학생용 홈에 `published: true` 인 교사 카드를 늘어놓을 수
있다. `teacherPages` 가 공개 읽기라 추가 비용이 없다. 홈 → 선생님 → 과목 순서로
찾아가게 된다.

---

## 5. 권한 규칙 (firestore.rules)

**프론트엔드 검사는 장식이고 진짜 방어선은 여기다.** 규칙은 하위 컬렉션으로
자동 상속되지 않으므로 `chunks` 같은 서브컬렉션도 **빠짐없이 따로 적어야 한다** —
빠뜨리면 파일 본문이 무방비로 열린다.

```
function isMember(wsId) {
  return request.auth != null
    && request.auth.token.email_verified == true
    && exists(/databases/$(database)/documents/workspaces/$(wsId)/members/$(request.auth.uid));
}
```

| 컬렉션 | read | write |
|---|---|---|
| `members` | 본인 문서만 `get` (목록 조회 차단 → 교사 이메일 유출 방지) | 콘솔에서만 |
| `settings/site`, `settings/club` | `true` | `isMember()` |
| `teacherPages/{slug}` | `true` | 본인 uid 문서만 (아래) |
| `courses`, `materials`(+chunks) | `true` | `isMember()` |
| `seasons`, `activities`(+files/slides/chunks) | `true` | `isMember()` |
| `presentations` | `true` | `isMember()` |
| `classes`, `students`, `dates` | **담당 교사만** (아래) | 담당 교사만 |
| `timetables/{uid}` | **`isMember()`** | 본인 uid만 |

**학생은 로그인이 없으므로 학생이 봐야 하는 것은 읽기를 열어둘 수밖에 없다.**
그래서 **핀번호는 "가벼운 잠금"이다** — 개발자도구로 Firestore를 직접 열면 핀 값도
보인다. CHICODE에서 사용자가 알고 받아들인 트레이드오프이고 차림도 같다.
진짜 서버 검증은 Blaze 유료 플랜 + Cloud Functions가 필요하다.
**이 트레이드오프를 몰래 "강화"하려고 하지 말 것.**

반대로 `classes` / `timetables`는 학생이 볼 이유가 없는 교사 업무 정보이고
학번·이름은 미성년자 개인정보라, **읽기까지 막는다.**

**`classes` 는 거기서 한 겹 더 좁힌다** — `isMember()` 만으로는 부족하고 그 반의
담당 교사여야 한다(4.5절 결정).

```
match /workspaces/{wsId}/classes/{classId} {
  allow read: if isMember(wsId)
              && request.auth.uid in resource.data.teacherUids;
  allow create: if isMember(wsId)
              && request.auth.uid in request.resource.data.teacherUids;
  allow update, delete: if isMember(wsId)
              && request.auth.uid in resource.data.teacherUids;

  // 하위 컬렉션은 규칙을 상속받지 않는다. 부모 문서를 get() 해서 다시 확인한다.
  match /students/{studentId} {
    allow read, write: if isMember(wsId) && request.auth.uid in
      get(/databases/$(database)/documents/workspaces/$(wsId)/classes/$(classId))
        .data.teacherUids;
  }
}
```

`get()` 은 요청당 문서 읽기를 한 번 더 쓴다(같은 요청 안에서는 캐시된다).
무료 한도가 하루 5만 읽기라 반 명단을 여는 정도로는 문제가 없지만, 규칙 안에서
`get()` 을 겹겹이 쓰지는 말 것.

**`teacherPages` 의 쓰기 규칙이 곧 슬러그 중복 방지 장치다**(4.6절).

```
match /workspaces/{wsId}/teacherPages/{slug} {
  allow read: if true;
  // 새 슬러그: 자기 uid 로만 만들 수 있다.
  allow create: if isMember(wsId)
                && request.resource.data.uid == request.auth.uid;
  // 이미 있는 슬러그: 그 주인만 고칠 수 있다.
  // 남이 쓰던 슬러그를 저장하려 하면 create 가 아니라 update 로 걸려 여기서 막힌다.
  allow update, delete: if isMember(wsId)
                && resource.data.uid == request.auth.uid;
}
```

**핀 입력 제한** — `pinThrottle.ts`가 실패 횟수에 따라 잠금 시간을 늘린다.
게이트마다 저장 키를 나눠야 서로 간섭하지 않는다(`'club'`, `courseId` 등).

---

## 6. 의존성

### 가져올 것

```
react  react-dom  react-router-dom  firebase
@dnd-kit/core  @dnd-kit/sortable  @dnd-kit/utilities   ← 탭·항목 드래그 정렬
pdfjs-dist  pptx-preview  jszip                         ← 발표자료 뷰어·대본 추출
prismjs                                                 ← 활동 코드블록 강조
tailwindcss  @tailwindcss/vite                          ← v4. @theme 로 색 토큰
vite  @vitejs/plugin-react  typescript  oxlint
firebase-tools                                          ← 규칙 배포
```

### 뺄 것

```
pyodide  monaco-editor  @monaco-editor/react            ← 실습
@micropython/micropython-webassembly-pyscript           ← Pico
@fontsource/jua                                         ← 치즈 브랜딩
rss-parser  firebase-admin                              ← 뉴스 자동 수집
```

`package.json`의 `postinstall`(sync-pyodide / clang / micropython / pdfjs)도
**pdfjs 하나만 남기고 지운다.**

### 폰트

`@fontsource/jua` 대신 **Pretendard를 직접 `@font-face`로 얹는다.**
`brand/pretendard/woff2/`에 서브셋 4종(Regular/Medium/SemiBold/Bold, 합계 1.05MB)이
이미 있다. `brand/pretendard/pretendard-subset.css`가 참고용 `@font-face` 예시다.

**외부 CDN을 쓰지 않는다** — 학교 네트워크가 CDN을 막을 수 있다는 전제.

### 색 토큰

Tailwind v4는 `src/index.css`의 `@theme` 블록에 CSS 변수를 선언하면 유틸리티가
생성된다. CHICODE의 `--color-cheese-*` 자리에 차림 팔레트를 넣는다.

```css
@theme {
  --color-primary: #315C8C;
  --color-primary-dark: #244461;
  --color-secondary: #7C9BB5;
  --color-bg: #F5F7FA;
  --color-surface: #FFFFFF;
  --color-text: #263442;
  --color-line: #E6EAF0;
  --color-success: #4E8068;
  --color-warning: #D59A35;
  --color-error: #C65B5B;
  --font-sans: 'Pretendard', -apple-system, BlinkMacSystemFont,
    'Apple SD Gothic Neo', 'Segoe UI', system-ui, sans-serif;
}
```

CHICODE에 있던 `--font-display`(Jua)는 **만들지 않는다.** 차림은 Pretendard
한 벌로 간다 — 브랜딩 가이드 7절.

---

## 7. 만드는 순서

앞 단계가 끝날 때마다 실제로 눌러보고 확인받은 뒤 다음으로 간다.

1. **골격** — Vite + React + TS + Tailwind v4, Pretendard 자체 호스팅, 색 토큰,
   로고·파비콘, `App.tsx` 레이아웃(헤더 탭 + 풋터)
2. **로그인** — `firebase.ts` + `AuthProvider` 이식, `isMember()` 규칙,
   `wsId` 상수 고정, 콘솔에서 본인 멤버 문서 생성.
   교사 페이지 설정 탭에 **내 주소(슬러그) 정하기**를 함께 넣는다(4.6절) —
   로그인이 되면 바로 만들 수 있고, 교사가 학생에게 알려줄 주소가 그때부터 생긴다
3. **일정** — 시간표(`timetables/{uid}`)부터. 개인정보가 없어 가장 먼저 실물이 나온다
4. **수업기록** — 반 명단을 학교 공용으로 분리해서 이식
5. **수업자료** — 과목 + 게이트 + `chunkedFile` 업로드.
   학생 쪽 `/t/:slug` 라우팅을 여기서 붙인다 — 과목이 있어야 목록에 띄울 게 생긴다
6. **동아리** — `labs.ts` + 보드 에디터 + 활동 상세. `labScope`로 과목 스코프와 공유
7. **발표 모드**
8. **홈** — 교사용/학생용 두 벌. 다른 화면이 다 있어야 홈에 뭘 모을지 정해진다
9. **정책 문서** — 실제 동작을 확인한 뒤 작성

**1~3번까지만 해도 쓸 만한 게 나온다.**

---

## 8. 이식할 때 흔히 틀리는 곳

CHICODE가 실제로 겪고 고친 것들이다. 그대로 베끼면 같은 버그를 다시 만든다.

1. **싱글턴 문서** — `timetable/default` 같은 고정 id는 교사가 여럿이면 서로
   덮어쓴다. `{uid}` 또는 `{wsId}`로 갈라야 한다.
2. **서브컬렉션 규칙 누락** — `firestore.rules`는 상속되지 않는다. `chunks`까지 적을 것.
3. **한글 탭 줄바꿈** — flex 아이템이 좁아지면 글자 단위로 쌓인다. 3절 참고.
4. **아이패드 Safari 로그인** — `browserLocalPersistence`를 지우면 재현된다. 4.1절 참고.
5. **Firestore 복합 색인** — `where` + `orderBy`를 같이 쓰면 색인을 요구한다.
   CHICODE는 정렬을 클라이언트에서 해서 피한다.
6. **`chunkedFile` 삭제 누락** — 메타 문서만 지우고 `chunks`를 안 지우면 용량이
   남는다. 무료 1GiB 한도에서는 치명적이다.
7. **base64 팽창** — 파일이 원본보다 약 37% 커진다. 업로드 크기 제한을 걸 때 감안할 것.
