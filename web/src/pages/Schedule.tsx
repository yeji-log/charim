import Placeholder from '../components/Placeholder'

export default function Schedule() {
  return (
    <Placeholder
      title="일정"
      description="요일 x 교시 시간표와 반별 수업기록. 시간표는 교사마다 각자 다릅니다."
      step="3단계에서 만듭니다 — 다음 차례"
    />
  )
}
